<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="esecforte.com.qrscanner">

    <uses-permission android:name="android.permission.CAMERA" />
    <application
        android:name=".QRCode"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScanResultActivity" />
        <activity android:name=".FullScannerActivity" />
        <activity android:name=".StoreActivity" />
        <activity android:name=".AddNewFixture" />
        <activity android:name=".StartScanActivity" />
        <activity android:name=".FullDetailActivity" />
    </application>

</manifest>